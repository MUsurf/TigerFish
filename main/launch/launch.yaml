%YAML 1.2
---
launch:
    # Launch the IMU system
    - include:
        file: "$(find-pkg-share imu)/launch/imu.xml"

######################
# todo: Document this
# */ Add this to the CMakeLists.txt of the package you're trying to include
# install(DIRECTORY launch
#   DESTINATION share/${PROJECT_NAME}
# )
######################
